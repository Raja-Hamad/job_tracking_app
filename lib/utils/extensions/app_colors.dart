import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2979FF);       // Buttons, highlights
  static const Color secondary = Color(0xFFE3F8F2);     // Light button background
  static const Color background = Color(0xFFF9F9F9);    // App background
  static const Color white = Color(0xFFFFFFFF);         // Cards, AppBar
  static const Color border = Color(0xFFE0E0E0);        // Divider lines, input borders
  static const Color textPrimary = Color(0xFF212121);   // Main text
  static const Color textSecondary = Color(0xFF757575); // Subtext
  static const Color error = Color(0xFFFF6B6B);          // Errors or delete icons
}
